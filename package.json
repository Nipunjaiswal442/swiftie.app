{
  "name": "swiftie-monorepo",
  "version": "1.0.0",
  "scripts": {
    "install:web": "cd mobile && npm install",
    "web": "cd mobile && npm run web",
    "build:web": "cd mobile && npm run build:web"
  }
}
